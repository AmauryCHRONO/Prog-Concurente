import os, time, random, sys
N = 3
for i in range(N) :
    #__________début des ajouts_________
    pid_fils=os.fork()
    pid_fils=os.fork()
    # __________fin des ajouts__________
print("Bonjour")
sys.exit(0)